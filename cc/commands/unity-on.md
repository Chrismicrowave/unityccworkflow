1. Set `.claude/unity-mode.json` `enabled` field to `true`.
2. Append to `.claude/unity-mode-history.jsonl`: `{"ts": "<iso-timestamp>", "action": "master-on", "session": "<session-id>"}`.
3. Confirm with: "Unity discipline enabled 🛡️"

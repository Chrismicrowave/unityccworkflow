Set all values in `.rules` to `true` in `.claude/unity-mode.json`. Update `lastChanged` and `changedBy` fields. Append to `.claude/unity-mode-history.jsonl`: `{"ts": "<iso-timestamp>", "action": "rule-reset-all", "session": "<session-id>"}`.

Confirm: "All 9 rules reset to enabled. Unity discipline fully active 🛡️"
